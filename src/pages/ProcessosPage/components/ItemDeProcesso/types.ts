import type { LinhaProcessoProps } from "../LinhaProcesso/types";

/** As mesmas props da linha de tabela: o item é a outra forma do MESMO
 * dado, e duas listas de props divergiriam no primeiro campo novo. */
export type ItemDeProcessoProps = LinhaProcessoProps;
