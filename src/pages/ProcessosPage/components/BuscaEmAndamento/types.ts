import type { ProcessoEncontrado } from "../../../../types";

/** O que a busca em segundo plano já achou, fundido página a página. */
export interface BuscaEmAndamentoProps {
  processos: ProcessoEncontrado[];
}
